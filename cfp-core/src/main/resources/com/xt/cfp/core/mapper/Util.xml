<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="utils">

	<!-- 每隔一段时间访问一下数据库 -->
    <select id="queryValiServerData" resultType="long">
    	SELECT 1 FROM dual
  	</select>

</mapper>